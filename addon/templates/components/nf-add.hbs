<LinkTo
  @params={{params}}
>
  <NfTooltip
    @icon={{icon}}
    class="btn-floating btn-large modal-trigger tooltipped waves-effect waves-light red"
    @text={{tooltip}}
    @position={{left}}
  />
</LinkTo>